{
  "routes": [
    {
      "method": "GET",
      "path": "/equips",
      "handler": "equip.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/equips/count",
      "handler": "equip.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/equips/:id",
      "handler": "equip.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/equips",
      "handler": "equip.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/equips/:id",
      "handler": "equip.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/equips/:id",
      "handler": "equip.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
